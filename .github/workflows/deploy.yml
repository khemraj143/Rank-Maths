name: FTP Deployment

on:
  push:
    branches:
      - main  # Trigger on push to main branch (adjust if you use a different branch)

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # Checkout code from the repository
    - name: Checkout code
      uses: actions/checkout@v3

    # Set up FTP deployment using actions
    - name: Upload to FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.1.0
      with:
        ftp-server: ftp://ftpupload.net
        ftp-username: icei_38523038
        ftp-password: PAiKLe7DSH2l
        local-dir: ./  # Path to the local files to upload (current directory in this case)
        server-dir: /htdocs  # Destination directory on the server

    # Optionally, you can add more steps to run tests, build, etc.
